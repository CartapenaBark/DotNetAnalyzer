<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\DotNetAnalyzer.Core\DotNetAnalyzer.Core.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <!-- 目标框架：net8.0 -->
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <!-- .NET Global Tool 配置 -->
    <PackAsTool>true</PackAsTool>
    <ToolCommandName>dotnet-analyzer</ToolCommandName>
    <PackageId>DotNetAnalyzer</PackageId>
    <Version>0.5.0</Version>
    <Authors>CartapenaBark</Authors>
    <Description>MCP server for .NET code analysis with Roslyn</Description>
    <PackageTags>mcp;roslyn;code-analysis;claude;dotnet</PackageTags>
    <PackageProjectUrl>https://github.com/CartapenaBark/DotNetAnalyzer</PackageProjectUrl>
    <RepositoryUrl>https://github.com/CartapenaBark/DotNetAnalyzer</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageReleaseNotes><![CDATA[
      v0.5.0 - SLNX Format Support & Roslyn 5.0 Upgrade
      - ✨ Support for Visual Studio 2022 .slnx XML format solution files
      - ⬆️ Upgrade Roslyn from 4.11.0 to 5.0.0
      - ✅ Backward compatibility with traditional .sln format maintained
      - ✨ Enhanced solution loading with dual format support (.sln and .slnx)
      - 🧪 Added comprehensive unit tests for .slnx support
      - 📝 Updated documentation with .slnx format information

      Breaking Changes:
      - Requires .NET 8.0 SDK or higher (Roslyn 5.0 requirement)

      Technical Details:
      - Upgraded Microsoft.CodeAnalysis.* packages to 5.0.0
      - Modernized WorkspaceManager API usage
      - Zero compilation warnings
      - All tests passing (4/4)

      Migration Guide:
      - No code changes required for existing users
      - .sln files continue to work as before
      - .slnx files are now automatically supported
    ]]></PackageReleaseNotes>

    <!-- 公司和产品信息 -->
    <Company>CartapenaBark</Company>
    <Product>DotNetAnalyzer - MCP Code Analysis Server</Product>
    <Copyright>Copyright (c) 2026 CartapenaBark</Copyright>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="8.0.0" />
  </ItemGroup>

  <!-- 包含项目文件到 NuGet 包 -->
  <ItemGroup>
    <!-- README.md 文件 -->
    <None Include="..\..\README.md" Pack="true" PackagePath="\" Visible="false" />

    <!-- LICENSE 文件 -->
    <None Include="..\..\LICENSE" Pack="true" PackagePath="\" Visible="false" />

    <!-- 图标（可选） -->
    <!-- <None Include="icon.png" Pack="true" PackagePath="\" Visible="false" /> -->
  </ItemGroup>

</Project>
